export type Request = {
    type_of_parcel?: string,
    user_id?: string,
    id?: string,
    from_city?: string,
    to_city?: string,
    type_of_package?: string,
    date_of_dispatch?: string,
    description?: string
}